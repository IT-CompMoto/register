export type RacerForm = {
    id: string;
    fullname: string;
    phone: string;
    email: string;
    team: string;
    number: number;
    image_url: string;
  };

  export type RacerField = {
    id: string;
  name: string;
  };


  export type InvoiceForm = {
    id: string;
    customer_id: string;
    amount: number;
  };

  